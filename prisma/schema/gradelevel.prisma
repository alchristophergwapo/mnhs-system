model GradeLevel {
  id               Int       @id @default(autoincrement())
  name             String    @unique
  gradeLevelNumber Int
  totalStudents    Int
  teachers         String[]
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt
  sections         Section[]
  subjects         Subject?
}
