import SectionsPage from "@features/academics/sections";

/**
 * @component Page
 * @description The default exported component representing the main page.
 * It renders the `SectionsPage` component as its primary content.
 * @returns {JSX.Element} The rendered SectionsPage component.
 */
export default function Page() {
    return <SectionsPage />
}
