/**
 * A component that renders a course item.
 * It renders a div element with the text "course item".
 * More content can be added here in the future.
 * @returns {JSX.Element} - The JSX element for the component.
 */
export default function CourseItem() {
    return <div>course item</div>
}