model News {
  id        Int      @id @default(autoincrement())
  title     String
  content   String
  thumbnail String?
  author    User[]
  authorId  Int
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}